---
title: "Docker 安装"
date: "2026-06-02"
tags: ["Docker", "服务器"]
---

今天在 Ubuntu 服务器上成功安装了 Docker。主要命令是 `curl -fsSL https://get.docker.com | sh`。安装完成后运行 `docker run hello-world` 验证。还学会了 `docker ps` 查看运行容器。